### on()

```text
	라이브 방식의 이벤트 등록
	아직 존재하지 않는 노드에 이벤트를 등록할 수 있다.
	html이 로딩된 후 동적으로 추가한 객체에 대해 이벤트를 미리 등록가능하다는 것 

	$대상.on("이벤트명", 이벤트리스너(){

	});

	$("#btnAdd").on("click",function(){


       $대상.on("이벤트명", "대상자식노드,이벤트리스너(){

    	});

	$("ul.menu").on("click","li",function(){	

```
